## 全局通用规则
- 项目需要在【工作目录】中保存必要的数据，MacOS和Linux系统默认为：$HOME/.local/share/fsak，Windows系统默认为为：%LOCALAPPDATA%\fsak。【工作目录】可以通过环境变量FSAK_WS_DIR来修改指定。
- 程序非正常退出时，退出码为1。
- 程序必须有help和version命令
- 编译后的可执行文件名称必须为go-fsak
- 不要做单元测试！